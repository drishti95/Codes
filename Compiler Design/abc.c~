#include <stdio.h>
int main()
{
long long int t,a,b,max,sum;
scanf("%lld",&t);
while(t--) // ahhffihi
{
/*yi
uiu*/
scanf("%lld %lld",&a,&b);
max=a;
if(b>a)
max=b;
			sum=a+b;
printf("%lld %lld\n",max,sum);
}
return 0;
}

